# Instructions

1. Create a list named `animals` with the names of at least two animals
2. Append the name of another animal to the end of the list using `.append()`
3. Insert the name of yet another animal so it becomes the first entry in the list using `.insert()`
4. Use the `+` operator to concatenate your list with another list. What is the result?
5. Try to print the value at an index that doesn't exist (e.g., index 99). What type of exception do you get?
6. Try to assign a value to an index that doesn't exist. What type of exception do you get?

Write your code in the cell below to complete the exercise.

```py-cell
# Write your code here

```
